name: Deploy to VPS

on:
  push:
    branches:
      - main
      - dev # temp for tests

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Get branch name
        run: echo "BRANCH_NAME=$(echo ${GITHUB_REF#refs/heads/} | tr '/' '-')" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login --username "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # Build & Push Web Image
      - name: Build & Push Web Image
        run: |
          docker build --target web -t ${{ secrets.DOCKERHUB_USERNAME }}/pocketour:web-latest -f Dockerfile .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/pocketour:web-latest

      # Build & Push Bot Image
      - name: Build & Push Bot Image
        run: |
          docker build --target bot -t ${{ secrets.DOCKERHUB_USERNAME }}/pocketour:bot-latest -f Dockerfile .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/pocketour:bot-latest

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e  # Exit immediately on error
            cd ~/pocketour/tour-telegram-bot

            echo "Pulling latest changes..."
            git pull origin main

            echo "Pulling latest Docker image..."
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/pocketour:latest

            echo "Restarting Docker containers..."
            docker compose down
            docker compose -f docker-compose.prod.yaml up -d --remove-orphans